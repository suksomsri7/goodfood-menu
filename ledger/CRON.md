# งานตามเวลา (cron) ของ goodfood — สภาพจริงบน VPS

> อัปเดต 25 ก.ย. 2569 · ที่อยู่จริง: `crontab -l` ของ root บน srv1403873
> ไฟล์นี้เป็น "สำเนาที่อ่านง่าย" ไว้ให้รู้ว่าอะไรวิ่งอยู่บ้างโดยไม่ต้องเข้าเครื่อง
> 🔴 กุญแจถูกปิดบังไว้ในไฟล์นี้ (รีโปนี้เปิดสาธารณะ) ค่าจริงอยู่ใน `.env.production` → `ARTICLE_CRON_SECRET`
> 🔴 เวลาใน crontab เป็น UTC — เวลาไทย = +7

| เวลา (UTC) | เวลาไทย | งาน | ทำอะไร |
|---|---|---|---|
| `30 22`, `0,30 23`, `0,30 0`, `0,30 1`, `0 2` | 05:30–09:00 ทุกครึ่ง ชม. | `/api/cron/morning-coach` | โค้ชทักตอนเช้า (ส่งคนละครั้ง/วัน) |
| `0 21 * * 0` | จันทร์ 04:00 | `/api/cron/weekly-adjust` | ปรับแผนรายสัปดาห์ ก่อนโค้ชเช้า |
| `*/30 2-14` | 09:00–21:00 | `/api/cron/nudges` | เตือนในแอป (จำกัด 3 ครั้ง/วัน) |
| `0 22` | 05:00 | `/api/cron/insights` | สรุปพฤติกรรมรายวัน |
| `30 21` | 04:30 | `/api/cron/cleanup` | ลบแจ้งเตือน/แชทเก่ากว่า 90 วัน |
| `0 1 * * 1` | จันทร์ 08:00 | `/api/cron/weekly-summary` | สรุปสัปดาห์ |
| `30 0 * * 1,4` | จ./พฤ. 07:30 | `/api/cron/weigh-reminder` | เตือนชั่งน้ำหนัก (ข้ามถ้าชั่งใน 3 วัน) |
| `30 12` | 19:30 | `/api/cron/article-push` | ส่งบทความให้สมาชิก |
| `10 22` | 05:10 | `/api/cron/video-sync` | ดึงคลิปใหม่จากช่อง YouTube |
| `15 * * * *` | ทุกชั่วโมง | `/api/cron/integrations-sync` | ดึงข้อมูล Fitbit/Strava |
| `0 2 1 * *` | วันที่ 1 เวลา 09:00 | `/api/cron/exercise-video-check` | เฝ้าคลิปสอนท่าที่ถูกลบ/ปิดฝัง |
| `0 4` | 11:00 | `/api/cron/pt-alerts` | เตือนแอดมิน (นิ่ง 3 สัปดาห์ / ความพร้อมต่ำ / เจ็บใหม่) |

รูปแบบคำสั่ง: `curl -s -H "X-Cron-Secret: <ARTICLE_CRON_SECRET>" https://goodfood.in.th/api/cron/<ชื่องาน> >> /var/log/goodfood-cron.log 2>&1`

## ⛔ ที่หยุดไว้ (ยังอยู่ใน crontab แต่ถูกคอมเมนต์)

| หยุดเมื่อ | งาน | เหตุผล |
|---|---|---|
| 17 ก.ย. 69 | `20 23` → `scripts/seed-videos-daily.sh` | เติมใบเข้าคิวตรวจคลิปทุกวันจนบวม 318 ใบ (ของ 7 ท่าที่ยังไม่มีคลิป) และเผาโควตาค้น YouTube ทุกวันทั้งที่ไม่มีใครกดตรวจ |
| 17 ก.ย. 69 | `0 8` → `POST /api/telegram/foodie/decide {"link":"หามาเพิ่ม"}` | เหตุผลเดียวกัน (ตัวสั่ง "หามาเพิ่ม" รายวัน) |
| 28–29 พ.ค. 69 | `publish-scheduled-articles`, `dispatch-social-posts`, `generator.sh`, `refill-topics.sh` | เจ้าของสั่งปิด / เปลี่ยนไปใช้ flow อนุมัติแบบใหม่ |

**เปิดคืนยังไง:** `crontab -e` แล้วลบเครื่องหมาย `#` กับข้อความในวงเล็บเหลี่ยมหน้าบรรทัดนั้นออก
· สำรอง crontab ก่อนแก้เสมอ: `crontab -l > /root/crontab-backup-$(date +%F).txt`
· ก่อนเปิด `seed-videos-daily.sh` คืน ควรเคลียร์ใบค้างในคิวก่อน ไม่งั้นบวมซ้ำ

## ดูผลการทำงาน
```bash
tail -f /var/log/goodfood-cron.log      # ไฟล์เดียวรวมทุกงาน (เขียนต่อท้ายไม่มีตัวตัด — โตเรื่อย ๆ)
```
